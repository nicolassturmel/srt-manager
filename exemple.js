const { SRT } = require('@eyevinn/srt');

//const srt = new SRT();
var socket //= srt.createSocket();
//srt.bind(socket, "0.0.0.0", 1234);
//srt.listen(socket, 2);
//const fd = srt.accept(socket);
if (0) {
  const chunk = srt.read(fd, 1316);
}